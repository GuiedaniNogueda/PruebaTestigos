<%= form_for [@order, @creative] do |f| %>
  <% if @creative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creative.errors.count, "error") %> prohibited this creative from being saved:</h2>

      <ul>
      <% @creative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :creative_name, "Nuevo Creativo", class: "col-md-3 control-label"%>
  </div>
  <div class="col-md-6">
    <%= f.text_field :creative_name, class: "form-control" %>  
  </div>
  <div class="actions">
    <%= f.submit "Aceptar", :class=>'btn btn-primary pull-right'%>
  </div>
<% end %>